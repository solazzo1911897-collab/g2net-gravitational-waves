# --- Python & System ---
__pycache__/
*.py[cod]
.venv/
.DS_Store

# --- Project Data ---
# 1. Ignore the dataset zip if present
*.zip

# 2. Ignore the heavy train folder (Keep this folder on your disk, but Git won't see it)
data/raw/train/

# 3. Explicitly ALLOW the labels CSV (We need this one)
!data/raw/training_labels.csv

# --- Outputs ---
# Ignore saved models (too heavy for standard GitHub)
outputs/models/

# Ignore LOCAL run artifacts (Generated by debug runs, we don't want these on GitHub)
outputs/logs/oof_predictions_b2.csv
outputs/logs/training_history.json

# --- Whitelist ---
# Ensure we track the Kaggle demo files and plots
!outputs/logs/
!outputs/plots/